1.- Only the API is compiled, validate that the version of .net core you have installed is compatible with the project version.

2.- The configuration that was made for the database was with SQLite, its basic operations of upsert, 
delete and insert were performed, to be able to manipulate the database it can only be done from the API, since it is a local database.